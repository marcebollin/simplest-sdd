# Grabim session — How Simplest SDD works

This session contains 4 annotated images. The attached images appear in the same order as the sections below.

## Image 1 — How Simplest SDD works

Page: http://localhost:3010/how
Captured area: 1270 × 461px

1. [Captured area · open] I want to reduce spaces and integrate the type selector from install, update and remove to the bottom component
   Location: Entire captured area

2. [<div> · open] reduce the gap at half between the bottom 3 informational data cells and the buttons to see prompt info

Remove the divider and move the mode selector (install, update, remove)

to be above the explanation paragraph and beign part of that component while having the same gap that the paragraph component and buttons has with the bottom 3 cells explanation
   Location: 32% left, 74% top
   Target context:
   Element: `<div class="prompt-overview" data-astro-cid-xddc5n57="">Install: Creates the project-specific skill, its approval gates, and the reusable specification workflow future agents w</div>`
   Selector: `main > section:nth-of-type(2) > div.prompt-overview`
   Component tree: React component metadata unavailable (DOM fallback included)
   Visible text: Install: Creates the project-specific skill, its approval gates, and the reusable specification workflow future agents will follow. Every color marks a different responsibility. Ho

3. [Arrow · open] No note added
   Location: x 24–41%, y 19–30%

## Image 2 — How Simplest SDD works

Page: http://localhost:3010/how
Captured area: 1311 × 427px

No notes on this image.

## Image 3 — How Simplest SDD works

Page: http://localhost:3010/how
Captured area: 1581 × 466px

1. [Captured area · open] Currently when reaching the end of the scroll of the component, if the sidebar has also an scrollbar because it is bigger than the viewport Y it isnt scrolling more to reach the latest part of the sidebar elements, please fix it to be sync and when reaching the end of the scroll page should also scroll to the end of the sidebar
   Location: Entire captured area

2. [<a> · open] this a is to big, should be fit content so the only clickable part is the logo
   Location: 26% left, 78% top
   Target context:
   Element: `<a href="/" class="footer-mark" data-astro-cid-xddc5n57="">(sd)2</a>`
   Selector: `section:nth-of-type(2) > div.prompt-layout > div.prompt-sections > div.prompt-end > a.footer-mark`
   Component tree: React component metadata unavailable (DOM fallback included)
   Visible text: (sd)2

3. [<div> · open] No note added
   Location: 49% left, 79% top
   Target context:
   Element: `<div class="prompt-end" data-astro-cid-xddc5n57="">(sd)2Read the skill. Then let the agent read it.What is this? →</div>`
   Selector: `main > section:nth-of-type(2) > div.prompt-layout > div.prompt-sections > div.prompt-end`
   Component tree: React component metadata unavailable (DOM fallback included)
   Visible text: (sd)2Read the skill. Then let the agent read it.What is this? →

4. [<div> · open] reduce the padding of this component and half on top and remove it directly on the bottom, also reduce the divider above of it
   Location: 43% left, 79% top
   Target context:
   Element: `<div class="prompt-end" data-astro-cid-xddc5n57="">(sd)2Read the skill. Then let the agent read it.What is this? →</div>`
   Selector: `main > section:nth-of-type(2) > div.prompt-layout > div.prompt-sections > div.prompt-end`
   Component tree: React component metadata unavailable (DOM fallback included)
   Visible text: (sd)2Read the skill. Then let the agent read it.What is this? →

5. [<p> · open] change this to bring me up button that should scroll to the top of the page (make sure that the label sounds naturally)
   Location: 54% left, 77% top
   Target context:
   Element: `<p data-astro-cid-xddc5n57="">Read the skill. Then let the agent read it.</p>`
   Selector: `section:nth-of-type(2) > div.prompt-layout > div.prompt-sections > div.prompt-end > p`
   Component tree: React component metadata unavailable (DOM fallback included)
   Visible text: Read the skill. Then let the agent read it.

## Image 4 — How Simplest SDD works

Page: http://localhost:3010/how
Captured area: 1119 × 505px

1. [<h1> · open] change this copy. Something like: because we should read the skills we install, i made it easy for you (tune it to sound naturally)
   Location: 44% left, 57% top
   Target context:
   Element: `<h1 id="how-title" data-astro-cid-xddc5n57="">Because we should read the skills we install, I added colors.</h1>`
   Selector: `#how-title`
   Component tree: React component metadata unavailable (DOM fallback included)
   Visible text: Because we should read the skills we install, I added colors.

Use every attached annotated image as a visual source of truth. Address every open numbered note and preserve unrelated UI.